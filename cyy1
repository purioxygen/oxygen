#include<stdio.>
int main()
{
    int num1=1
    int num2=3
    int sum=0(c语言语法规定，变量要定义在当前代码块最前面）
    scanf("%d%d",&num1,&num2);         //&是取地址符号
    sum=num1+num2
    printf("sum is %d\n",sum);
    return 0
}
